# 实时屏幕翻译 · 自捕获校验改为同轮当场对比 · v1.15.4 → v1.15.5

用户实测截图里出现了 v1.15.4 新加的一句提示：

> ⚠️ 无法避免截到自己
> 已放宽检测；若译文开始自我循环，请把翻译区域挪开叠层

**这句话本身就是故障证据**：它意味着"自捕获校验"连续误报了 8 次以上，
进而**每一轮都 `return`** —— 用户看到的就是"只翻译了第一句、之后一直不动"。

---

## 误判为什么必然发生

旧做法分两步：

1. 每出一段译文后，记住"**叠层可见时**抓到的帧"的指纹（`pollutedSig`）；
2. 之后每一轮拿新抓的帧去比：**像它就判定抓到了自己**，跳过。

问题出在第 1 步记住的东西上：**叠层是半透明的**（`panelAlpha` 本来就允许调低，
而且我复用了"翻译结果面板"的透明度偏好）。半透明的结果是：

```
藏起来抓的那一帧 ≈ 游戏原文
露出来抓的那一帧 ≈ 游戏原文 + 一层很淡的译文
                        ↑ 两者差异很小
```

于是"新抓的干净帧"与"污染的样貌"**天然就很像** → 每轮都误判成抓到自己 →
每轮 `return` → 8 次后放宽 → 提示语出现，同时流程已经空转了很久。

**这是我把"半透明面板"和"靠差异判断隐藏是否生效"这两件事放在一起的后果** ——
前者让后者失去了可判别性。

---

## 改法

### 1. 校验改成**同一轮内当场对比**，不再跟过期指纹比

每一轮：

```
藏起叠层 → 等 hideWaitMs → 抓 A
露起叠层 → 等 VERIFY_VISIBLE_MS → 抓 B
当场比 A 与 B：
   差异明显  → "藏"确实生效 → A 是干净游戏画面 ✅
   几乎一样  → "藏"根本没生效 → A 里含着我们自己的译文，跳过并加大等待
```

同一轮的两帧自比，**不存在"跟过期样貌比"的误判**，也不需要维护那份指纹。
另外只有连续 `POLLUTED_MAX_HITS = 8` 轮都真的藏不掉时才会放宽并提示。

代价：每轮多抓一帧（多约 160ms）。取帧间隔默认 1000ms，可以接受。

### 2. 叠层改成**接近不透明**

这条是根上的修复，有两个硬理由：

1. 它是"**原位覆盖**"——半透明就会**同时看到原文和译文**，两边都读不清；
2. 上面那套"藏/露两帧差异"的判据，**必须有足够差异才成立**。

所以实时翻译的叠层不再复用"面板透明度"偏好，直接压到 `≥0xF0`。
（拍照翻译那套贴片仍按原偏好走，不受影响。）

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `service/LiveTranslateService.kt` | 自捕获校验改为同轮 A/B 对比（`visibleSig` + `VERIFY_VISIBLE_MS`）；删除 `pollutedSig` 与 `rememberPolluted()`；`pollutedHits` 语义改为"连续真的藏不掉" |
| `overlay/LiveOverlayView.kt` | `applyPanelAlpha` 下限从 `0x60` 提到 `0xF0`（原位覆盖必须真的盖住） |
| `app/build.gradle.kts` | `versionCode 30→31`、`versionName 1.15.4→1.15.5` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=31 versionName=1.15.5`。

### 请验证

1. **叠层应当把原文完全盖住**（不再同时看到日文和中文）。这一条是这次改动的直接效果。
2. **连续出 4~5 段对白**，每段都要出译文。**不应**再看到「无法避免截到自己」那句提示。
3. 若又出现「⚠️ 隐藏叠层无效」→ 那是真的藏不掉（极少见），按提示把翻译区域挪一下即可。

### 从你的截图里我另外注意到两件事

- **位置看着已经对了**：叠层正好盖在 1811 的对话框上（GBA 画面 y≈55..414，
  对话框 y≈285..414，叠层边界与之吻合）。如果你看到的偏差还在，请用控制页的
  **X / Y 微调**校正，并把数值告诉我。
- 第二张截图里译文是 `体力提升了50`，而对话框显示的是 `君、ちょっといい？`
  —— 这像是**选区里混进了状态栏区域**（体力/タフ/やる気/リスク 那一排），
  模型把状态栏的数字也读进去并翻译了。建议**把选区收窄到只框对话框的文字行**，
  译文会干净很多。

# 实时屏幕翻译 · 变化检测重写 · v1.15.2 → v1.15.3

反馈：**"能识别到文字，但还是不变。"**

这句话同时解决了两件事：

1. **从最初就悬着的核心疑问有答案了：视觉模型确实能读 GBA 那种 8×8 点阵日文假名。**
   （这条路线成立。之前所有"识别不到"的现象，都不是模型的锅。）
2. "还是不变"于是只能是**变化检测**的问题 —— 模型读得出，但那段文字**从来没被送去翻译**。

顺着这条线回查，找到了真正的元凶，而且它是 v1.15.0 就带进来的**算法缺陷**。

---

## 元凶：指纹分辨率太低 + 用平均差

### 两个错误叠加

**错误一：16×16 太小，文字被抹平**

`FrameSignature` 把选区降采样成 **16×16 = 256 格** 的灰度图。而一个对话框选区
大约是 **1000×280 像素** —— 压到 16×16 后：

```
每格平均了 (1000/16) × (280/16) ≈ 62 × 17 ≈ 1000 个源像素
```

一个 8×8 的日文字符在里面只占**几分之一格**。文字笔画被彻底平均掉，
整张指纹退化成"一片均匀的灰"。**两段完全不同的日文，在 16×16 上几乎是同一个东西。**

**错误二：用"全图平均差"，被背景稀释**

`diff` 原来算的是 256 格的**平均**绝对亮度差。选区里通常 60%~90% 是**不变的背景**
（文本框边框、面板底色），文字只占一小块。取全图平均：

```
文字的差异 ÷ 整个选区面积  →  被摊薄到阈值(8)以下
```

两处叠加的后果：**换了一段新对白 → 指纹差异仍然 < 8 → 判定"画面没变" → 不发请求。**
表现就是"模型明明能读，但译文一直不变"。

而且这个缺陷和之前那些（自捕获污染、稳定判定）**长得一模一样**，所以前面几轮
按症状修都没修到点子上——这也解释了为什么改了两版还是"不变"。

---

## 改法

### 1. 分辨率 16×16 → **32×32**（1024 格）

平均窗口小 4 倍。每个日文字符在指纹里能占到 1~2 格，文字结构留得住。

### 2. 度量方式：**平均差 → "明显变了的格子占比"**

```kotlin
// 只有 |Δ| ≥ 24 灰阶的格子才算"这一格变了"，然后数占比
diff = 变了的格子数 × 100 / 总格子数
```

为什么这样更好：**不变的背景贡献 0**，只有真正发生变化的文字区域贡献比例。

典型取值（新度量下）：

| 画面状态 | diff | 判定 |
|---|---|---|
| 完全没动 / 只有传感器抖动 | 0 ~ 1 | 没变 |
| 同一句话 + 对话框里**闪烁的 ▼ 光标** | 0 ~ 1（只有几个格子动） | 没变 |
| **换成新的一段对白** | **10 ~ 40** | **变了 → 发请求** |

所以阈值 8（= 8% 的格子变了）能把"新文本"和"光标在闪"干净地分开 ——
既不会漏掉新对白，也不会因为光标闪烁而反复烧额度。

> 这也顺带说明：v1.15.2 里那个"稳定判定被闪烁光标打断"的担心，
> 在正确的度量下其实不成问题（光标的变化量远低于格子阈值），
> 真正的问题始终是这个"看不出来变化"的指纹。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `util/FrameSignature.kt` | **重写度量**：`N 16→32`；`diff` 从"全图平均差"改为"Δ≥24 的格子占比(%)"；新增 `CELL_DELTA` |
| `util/Prefs.kt` | `liveDiffThreshold` 的注释同步为"明显变了的格子百分比"，并写明典型取值 |
| `app/build.gradle.kts` | `versionCode 28→29`、`versionName 1.15.2→1.15.3` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=29 versionName=1.15.3`。

### 请重点验证

**让游戏连续出好几段对白，每换一段译文都要跟着变。**

- ✅ 每段新对白在 1~2 个取帧间隔内出译文
- ✅ 同一段对白停着不动时，**不应**反复发请求（通知里「已翻译 N 次」不涨）
- ❌ 若仍不变：把「画面变化阈值」调到 **2** 再试一次。若调到 2 就正常了，
  说明你的选区特别大（文字占比很低），可以把选区画小到只框文字框

### 至此仍未验证的

只剩"翻译质量"本身：**译文准不准**。这是模型对点阵字形的推断能力问题，
不是流水线问题。如果出现明显的误译/漏译，可行的办法是把选区画得**只框一行文字**
（上下文更干净），或用「画面变化阈值」配合更快的取帧间隔。

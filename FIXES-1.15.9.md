# 实时屏幕翻译 · 拖动摆放译文框 · v1.15.8 → v1.15.9

需求：**"加一个拖动"**。动机很实在 —— 用户要**换别的模拟器试**，而不同模拟器的画面
布局差很多（有的画面占满、有的只占上半屏、虚拟按键位置也各异），靠 X/Y 滑杆
一点点凑不现实。

---

## 设计：临时拖动模式

叠层默认是 `NOT_TOUCHABLE` —— 这是刻意的，否则它覆盖哪块就把哪块的游戏操作变哑。
要让它能拖，就必须临时变成可触摸。所以做成**临时模式**：

```
点「✋ 拖动调整译文框位置」（控制页按钮 或 通知栏「✋ 移动译文框」）
   ↓ 叠层变成可拖，显示"✋ 拖动我"
拖到不挡画面的位置，松手
   ↓ 立刻保存并自动退出拖动模式（恢复不吃触摸）
   ↓ 若 12 秒没拖完，也自动退出（DRAG_TIMEOUT_MS）
```

这样既不长期挡住游戏操作，又能自由摆到任意位置。

### 保存方式：反算"微调"

拖动时不引入新的数据结构，而是把**松手时的绝对坐标**换算成相对"自动摆放"基准的偏移：

```
微调 = 拖到的位置 − 自动摆放的位置
```

好处是：将来自动摆放策略再改进（比如针对不同模拟器调整默认位置），
用户之前拖出来的**相对**位置仍然有效，不会被一次策略调整洗掉。

为此把"自动摆放"和"微调"在代码里拆开了：

| 方法 | 职责 |
|---|---|
| `autoPlacement(roi)` | 纯策略：按选区与模式算出基准矩形（**不含**微调） |
| `displayRect(roi)` | `autoPlacement` + 用户微调 → 叠层实际摆放位置 |

---

## 入口有两个

| 入口 | 位置 | 说明 |
|---|---|---|
| 控制页按钮 | 屏幕翻译 → 🖼 实时屏幕翻译 → 「✋ 拖动调整译文框位置」 | 点完自动退到后台，游戏露出来 |
| 通知栏动作 | 展开「🖼 实时屏幕翻译」通知 | 「✋ 移动译文框」，**不用切出游戏** |

---

## 顺带回顾：这一版对"换模拟器"这件事是有利的

如果换到**画面占满全屏**的模拟器（没有黑边），贴边模式可能找不到不遮画面的位置，
那时有两个选择：
- 打开「原位覆盖原文」（观感最接近原生汉化，代价是会闪）
- 用拖动把译文框摆到画面里相对空的地方（比如天空、球场上方）

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `overlay/LiveOverlayView.kt` | 新增拖动模式：`setDraggable()`（临时可触摸 + 手势处理）、`applyTouchable()` 改为综合"拖动中 / 用户开关"；用 `rawX/rawY` 算增量（用 `x/y` 会随窗口移动产生越拖越快） |
| `service/LiveTranslateService.kt` | 新增 `ACTION_DRAG` / `enterDragMode()` / `saveNudgeFromDrag()` / `DRAG_TIMEOUT_MS`；把摆放拆成 `autoPlacement()` + `displayRect()`；删除已无用的 `overlayRect()`；通知新增「✋ 移动译文框」动作 |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | 新增「✋ 拖动调整译文框位置」按钮 |
| `app/build.gradle.kts` | `versionCode 34→35`、`versionName 1.15.8→1.15.9` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=35 versionName=1.15.9`。

### 请验证

1. 控制页点「✋ 拖动调整译文框位置」→ 应退到后台并看到叠层写着「✋ 拖动我」
2. **按住叠层拖动** → 应跟着手指走
3. **松手** → 叠层应显示「位置已保存 X …dp · Y …dp」，并且**再点它不再能拖**（已退出拖动模式）
4. 12 秒不操作 → 也应自动退出拖动模式
5. 切到别的模拟器后：重新框选区域 → 拖动摆放（这轮的主要使用场景）

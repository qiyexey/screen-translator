# 实时屏幕翻译 · 叠层不再闪烁 · v1.15.6 → v1.15.7

反馈：**"翻译框一直在闪，不行啊。"**

这条是我前面一个判断错了 —— 我在注释里写过"180ms 用户依然无感"，**实测就是会闪**。
根因不在时长，而在**架构**：为了拍到没被自己盖住的游戏画面，我每轮都把叠层藏掉一次。

---

## 为什么必然会闪

原位覆盖的叠层压住了选区，而选区正是我们要截取的地方。要拍到下面的游戏画面，
就只能**每轮把叠层藏起来一小会儿**：

```
藏 180ms → 抓帧 → 恢复
      ↑ 1 秒一轮，于是叠层每秒消失 180ms
```

调小时长并不解决根本问题：

| 隐藏时长 | 后果 |
|---|---|
| 180ms（现状） | **肉眼可见地闪** |
| 50ms | 闪得轻些，但合成器未必来得及把 alpha 落下去 → 可能抓到自己的译文 |
| 更短 | 基本等于没藏 |

**只要叠层盖住选区，"稳定取帧"和"不闪"就是互斥的。**

---

## 改法：让叠层**根本不盖住选区**

把"**截哪里**"和"**译文显示在哪**"这两件事解耦：

| 模式 | 叠层位置 | 是否遮挡选区 | 取帧要隐藏吗 | 会闪吗 |
|---|---|---|---|---|
| **`edge`（新默认）** | 贴在选区**外侧**（上方空间够就放上方，否则放下方） | ❌ 不遮 | ❌ 不需要 | ✅ **完全不闪** |
| `cover`（可选） | 原位盖住选区 | ✅ 遮 | ✅ 每轮要藏 | ⚠️ 会闪 |

默认切到 `edge`，于是：

- **不闪**：叠层从不需要隐藏，取帧始终是干净的游戏画面；
- **"截到自己"这一整类问题直接消失** —— 自捕获校验、污染指纹、自适应等待
  这些复杂机制在有遮挡时才有意义，现在默认路径根本用不上；
- **同时更可靠**：少了一整条会出错的链路。

想要"译文压在原文上"那种观感（更接近原生汉化）的用户，可以在控制页打开
「**原位覆盖原文（会周期性闪烁）**」—— 代价如实写在开关说明里，由用户自己取舍。
切换会即时生效（`ACTION_REPOSITION`），不用重开服务。

---

## 这次改动同时也修掉了一个理论风险

`edge` 模式下叠层不遮挡选区，于是**取帧永远不会包含我们自己的译文**。
前几版为了处理"遮挡"，引入了自捕获校验、污染指纹、同轮 A/B 对比、自适应等待——
这些机制在遮挡场景下是必需的，但也是**故障来源**（v1.15.4 的误报、v1.15.5 的误判
都出在这条链上）。默认路径改掉后，这条链整体退居二线。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `util/Prefs.kt` | 新增 `liveOverlayMode`（`edge` 默认 / `cover`）；新增 `LiveOverlayMode` 常量对象 |
| `service/LiveTranslateService.kt` | 新增 `displayRect()`：把"译文显示矩形"与"选区"解耦，`edge` 模式自动选上下方空间更大的一侧；四处 `applyGeometry` 改用它；`needHide` 仅在 `cover` 模式成立 |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | 新增开关「原位覆盖原文（会周期性闪烁）」，切换即时重摆叠层 |
| `app/build.gradle.kts` | `versionCode 32→33`、`versionName 1.15.6→1.15.7` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=33 versionName=1.15.7`。

### 请验证

1. **叠层不应再闪**（默认贴边模式）。译文会出现在文字框的**上方或下方**，
   不再压在原文上。
2. 文字框连续换几段，译文要跟着变（这次取帧完全不受遮挡影响，应该最稳）。
3. 若你更想要"原位覆盖"的观感：打开那个开关，确认一下你说的"闪"是不是就是它
   —— 以便确认我们对"闪"的定位一致。

> 若贴边模式下译文位置不合适（被顶部状态栏挤住、或与虚拟按键重叠），
> 用控制页的 **X / Y 微调**挪到你看着舒服的位置，把数值告诉我，
> 我可以把它固化进默认摆放策略。

# 拍哪译哪 · 译文盖住原文 · v1.13.0 → v1.14.0

拍照翻译从「**整屏文字 → 一坨译文**」改成「**译文盖在它原来所在的位置上**」，
并且**按住屏幕可以随时看回原文**（谷歌拍照翻译的手感）。

---

## 问题：译文和原文位置完全脱钩

v1.13.0 的拍照翻译是这样跑的：

```
拍照 → 整屏 OCR → 所有行拼成一整段文本 → 发一次翻译 → 译文显示在底部卡片里
```

`OcrEngine.recognize()` 其实**一直在返回每一行的位置**（`Line(text, box)`），
但从 `toPlainText()` 那一刻起，位置信息就被丢掉了。结果：

- 菜单、路牌、表单这类**多行**内容，用户拿到一坨译文，**对不上哪句对应哪行**；
- 想只翻某一行（比如菜单里第二道菜）做不到，只能整屏翻一遍；
- 底部卡片还会挡住取景画面，边看边读很别扭。

一句话：**识别是有位置的，翻译却是没位置的**。

---

## 改法

### 1. 拍哪译哪（点选就地翻译）

取景时**直接点屏幕上的一行字**：

```
点击 (view x,y) → 拍下这一帧 → 端侧 OCR → 反查「包含该点的最小文字框」
              → 只翻这一行 → 译文贴回这一行的框上
```

- 判点在框内 → 取**面积最小**的那个框（嵌套时最贴合的那一层）
- 没点进框 → 取 **40dp** 内最近的一行，点偏一点点也能用
- 冻结帧上继续点其他行，一行一行补齐，互不干扰

### 2. 译文盖住原文（像谷歌拍照翻译那样）

贴片**不透明**，宽高都不小于原文框 —— 直接把原文盖掉，"看到的就是译文"，
而不是"原文旁边多了个气泡"。底色按原文框内的平均亮度取样自适应：
浅底用浅色块 + 深色字，深底反过来，免得白底菜单上糊一块黑。

**按住屏幕 → 译文全部消失，露出原文；松手恢复。** 这是核对原文的手势
（`PEEK_DELAY_MS = 220ms`，比系统长按 500ms 短，手感跟得上手指）；
按住过就不再把这次抬手当成点选，两个手势不会打架。

### 3. 快门 = 整屏逐行贴合

所有识别到的行各自成一次翻译，译文各自盖回原位。

**为什么逐行而不是整段**：整段译文只有一坨文字，要拆回各行只能靠“行数对齐”猜——
而引擎完全可能合并、重排、改写行结构，**拆错就等于把 A 行的译文贴到 B 行上**，
比原来的“没位置”更糟（错得还很隐蔽）。逐行的位置是**确定的**。

代价是行数多时请求数多，所以三重控制：

| 手段 | 效果 |
|---|---|
| **同文去重** | 菜单里重复出现的「￥38」这类行只发一次请求，结果分发给所有同文行 |
| **并发上限 4**（`MAX_PARALLEL`） | 太高会被引擎限流（429），太低整屏要等很久 |
| **v1.10.0 翻译缓存** | 原文命中直接返回，重复翻译同一张图/同一句 = 零请求零费用 |

### 4. 需要上下文时：📄 全文

逐行翻译的短句没有上下文（“Bank” 是银行还是河岸？）。底部保留 **「📄 全文」**：
把整屏原文**一次**发给引擎，带上下文的连贯译文出现在原来的卡片里。
两种模式各有所长，都留着，由用户选。

### 5. 冻结帧：位置必须静止

预览是活的。译文贴在原位后如果继续显示实时画面，手一抖贴片就跟着跑，
用户会认为“又贴歪了”。所以拍照后立刻把这一帧**冻结**（`freezeFrame`，
`ivFrame` 盖住 `PreviewView`），位置静止，可以慢慢读、慢慢点。

### 6. 坐标系：与取景框一致

- 贴片定位用 `frameTransform()`：**`FILL_CENTER`（max 缩放 + 居中裁切）**，
  与 `PreviewView` 的规则**完全一致**。用 `FIT_CENTER`（min）算会整体偏一圈留白。
- OCR 在**缩小图**上做，框按 `原图宽 / 缩小图宽` 放大回原图坐标系，否则贴片整体偏左上。
- 预览与拍照**锁定同一宽高比**（`AspectRatio.RATIO_4_3`）：两者宽高比不同时，
  取景裁切范围与照片裁切范围不一致，就会出现“点这里、翻到旁边那行”。

### 7. 先缩再冻（内存）

冻结帧要常驻到用户重拍，所以只保留**最长边 ≤ 2000px** 的那一张，
相机原图（12MP ≈ 48MB ARGB_8888）立刻 `recycle()`。这张冻结帧同时充当 OCR 输入，
于是识别框与贴片**天然同坐标系**，连换算都省了 —— 省内存和少一处出错点，是同一次改动。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `ui/CameraTranslateActivity.kt` | **重写**（436 → 约 1000 行）：冻结帧、`LineLayer` 点选层、译文贴片、逐行并发翻译、全文面板保留 |
| `ui/OnboardingActivity.kt` | 引导文案：`📷 对着实物/菜单拍照` → `📷 拍照：点哪一行就译哪一行` |
| `README.md` | 模式表「拍照翻译」一行补上贴原文/点选说明 |
| `app/build.gradle.kts` | `versionCode 24→25`、`versionName 1.13.0→1.14.0` |

新增的界面元素（全部程序化构建，未新增布局文件）：`ivFrame`（冻结帧）、
`LineLayer`（未译行细框 + 点选手势）、`overlayHost`（译文贴片容器）、
底部新增「📄 全文」「🔄 重拍」。

---

## 验证

1. 打开 拍照翻译 → **点菜单里的某一行** → 译文应**盖住那一行原文**（其余行只有细白框）
2. 再点另一行 → 那一行也被译文盖住，第一处不受影响
3. **按住屏幕** → 所有译文消失、露出原文；**松手** → 译文回来
4. 按快门 → 整屏每一行的原文都被各自译文盖住；状态栏显示 `已就地翻译 n/m 行`
5. 按「📄 全文」→ 底部卡片出现整段译文（一次请求，带上下文）
6. 「🔄 重拍」→ 回到取景，贴片清空
7. 白底菜单 / 深底招牌各试一次：贴片底色应跟着变（浅底浅块深字、深底深块浅字）
8. 竖屏/横屏各试一次：译文**不应整体偏向某个角**（坐标系错位会集中体现在这里）

> 行数很多（>20 行）的整屏逐行翻译会发多次请求；若用的是按量计费的 LLM 引擎，
> 想省额度就点单行翻译，或直接用「📄 全文」（一次请求）。

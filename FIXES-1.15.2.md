# 实时屏幕翻译 · 译文不再"卡住" · v1.15.1 → v1.15.2

实测反馈的一条关键现象：**画面上文本已经换成新的一段了，叠层里的译文却一直不变。**

这条不是"偶发"，而是两个会**永久锁死**的缺陷。下面说清机理与改法。

---

## 缺陷 ①（主因）：抓到自己 → 参照帧被污染 → 永久判"没变"

### 机理

为了不把自己的译文截进去，取帧前会把叠层隐藏一小会儿（原为 50ms）再抓。但如果
**50ms 不够**（ColorOS 的合成器没来得及把 `alpha=0` 落下去），抓到的是**我们自己的译文**。
于是：

```
干净的旧文本 = 参照帧
     ↓ 翻译、叠层盖上
抓帧 → 其实抓到的是"我自己的译文" → 与参照帧一比：差很多 → 判定"变了"
     ↓ 下一轮
还是我自己的译文 → 与候选帧几乎一样 → 认账！
     ↓
参照帧 := 我自己的译文
     ↓
之后每一轮抓到的都是同一份译文 → 差值 ≈ 0 → 永远"没变"
     ↓
        【永久锁死】游戏文本一直在换，译文再也不会更新
```

关键点在于：**这个死锁是不可逆的**——一旦认账，参照帧就变成了自己的译文，
而自己的译文在叠层不被更新的情况下当然永远不变。用户看到的就是"文本变了、译文不变"。

### 改法

1. **记录"污染样貌"**（`pollutedSig`）：每出一段新译文后，特意**不隐藏叠层**抓一帧，
   把它的指纹记下来 —— 这就是"抓到自己"长什么样。
2. **每轮抓帧做自捕获校验**：如果这一帧与"污染样貌"几乎一致，就判定抓到了自己，
   **绝不认账**，直接跳过。
3. **自适应加大等待**：每次发现抓到自己，就把隐藏等待时间 +120ms（上限 800ms），
   直到能抓到干净画面。这样不同 ROM 上的合成器延迟都能自动适配。
4. **初始等待 50ms → 180ms**（约 11 帧@60Hz）：实测 50ms 在 ColorOS 上不够；
   180ms 用户依然无感（只占取帧间隔的一小部分）。

### 为什么不沿用"相邻两帧彼此接近"的稳定判定

原实现要求"检测到变化后，下一轮画面与这一轮仍然接近"才算稳定。这个判据在
**对话框里有闪烁的 ▼ 光标**时会很脆弱：光标一亮一灭，两帧永远不"接近"，
条件就永远攒不够 —— 同样表现为"译文停在上一段"。

现在改成 **连续 [CHANGE_STREAK]=2 轮都判定"变了"就认账**。看的是
"相对**旧参照帧**仍在变"，而不是"相邻两帧彼此接近"：
无论光标亮着还是灭着，与旧参照帧的差异都很大，所以对闪烁免疫。

> 副作用（如实说明）：如果选区内有**持续且幅度足够大**的动画（滚动的地图、闪烁特效），
> 会每隔两轮触发一次翻译。这时请把「画面变化阈值」调大，或把选区画小到只框文字。

---

## 缺陷 ②：从通知栏点「框选区域」会把通知栏冻成背景

点通知动作时**通知栏还开着**，服务立刻抓帧 → 冻下来的背景其实是通知栏，
而且 ColorOS 在屏幕采集期间会对通知内容做**脱敏**（整片黑）。所以框选器一打开
就是全黑，看着像"取帧坏了"。

改法：触发框选后先等 `PICKER_SETTLE_MS = 700ms` 让通知栏收起，再去抓冻结帧。

---

## 顺带查清的几件事（真机实测）

| 现场现象 | 结论 |
|---|---|
| 通知栏上看不到「框选区域」按钮 | **按钮一直在**（`[⏸ 暂停] [🔲 框选区域] [⏹ 停止]`）。ColorOS/Android 默认**折叠**通知动作，下拉展开即见。v1.15.1 的通知正文已写明"下拉通知展开「框选区域」等按钮" |
| ColorOS 弹出「屏幕共享保护已开启」 | 这是系统在**检测到屏幕采集时自动启用**的，不是用户的开关，**关不掉**。它主要脱敏通知等敏感内容；**游戏画面的 MediaProjection 采集不受影响** |
| 无障碍截屏整屏黑 | 屏幕采集进行中，ColorOS 会让**截屏通道**拿不到 App 内容（系统 UI 除外）。这只影响截图，不影响 MediaProjection 取帧 |
| 一台手机上装了哪个模拟器 | **My Boy!（`com.fastemulator.gba`）** |

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `service/LiveTranslateService.kt` | `candidateSig` 稳定判定 → `changeStreak` 连续计数；新增 `pollutedSig` 自捕获校验与 `hideWaitMs` 自适应；`CAPTURE_HIDE_MS` 50→180ms；新增 `rememberPolluted()`；框选前新增 `PICKER_SETTLE_MS` 等待 |
| `app/build.gradle.kts` | `versionCode 27→28`、`versionName 1.15.1→1.15.2` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=28 versionName=1.15.2`，v2 签名在位。

### 请重点验证（这次要看的就一条）

**让游戏连续出好几段对白，每一段叠层都要跟着变。**

- ✅ 每换一段新对白，译文在 1~2 个取帧间隔内更新
- ❌ 若仍然"文本变了译文不变" → 说明还有第三个原因，请告诉我当时
  「画面变化阈值」设的是多少、选区画了多大（选区越大，单行文字变化对整体
  差异度的贡献越小，可能低于阈值而被判成"没变"）

### 仍需你实测的核心疑问

**视觉模型到底认不认得了 GBA 那种 8×8 点阵日文假名。**
这一点从头到尾没有验证过：要么模型读得出来，要么它一律回"没有识别到文字"。
如果修完 ① 之后叠层能正常跟着文本更新，就说明模型读得出来；如果一直回"没有识别到文字"，
请把「画面变化阈值」调到 2、选区画小到只框一行，再试一次。

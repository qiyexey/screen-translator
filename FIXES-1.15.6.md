# 实时屏幕翻译 · 收紧提示词 + 加入"送出的图"回看 · v1.15.5 → v1.15.6

反馈：**"译文写着体力提升了50，但游戏内容根本不是这个。"**

这条不是"没更新"，而是**译文本身是编造的**。查下来，直接原因在我给游戏画面写的提示词里。

---

## 成因：我的提示词在**鼓励模型猜**

旧版 `GAME_HINT` 里有一句：

> 1. 对模糊字形要结合上下文（体育/棒球术语、人名、菜单用语）**推断，不要轻易输出"无法识别"**。

这句本意是"别动不动说不认识"，但实测效果是**教模型编**：

```
点阵字本来就看不清
   ↓ 提示词又说"要结合上下文推断、不许说无法识别"
模型顺着画面里的状态条（体力 / タフ / やる気 / リスク）把上下文补全
   ↓
给出一句**听起来非常合理的棒球术语**：「体力提升了50」
   ↓
用户看到的：一句自信的假话
```

编造比漏译更糟：**漏译用户看得出来，编造看不出来** —— 会直接把错误信息喂给玩家。

## 改法：把优先级反过来

新提示词明确四条硬约束：

1. **只翻译你确实看得清的字**；看不清宁可留空，**绝不根据数字/图标/状态条推测**出一句话。
2. 明确告知：**游戏状态条（体力、等级、局数、月份）不是对白**，不要把它们凑成一句话；
   只翻译文字框 / 对话框里的正文。
3. 译文要短，不保留日文原文、不加括号注释。
4. 整张图没有能确认的文字时，只输出：没有识别到文字。

原则从"尽量别说不认识"改成 **"宁可漏译一句，也不给一句自信的假话"**。

---

## 同时加入：**"最近一次送出的图"回看**

"译文和游戏画面对不上"有**两种长得一模一样**的可能，光看译文分不出来：

| 可能 | 本质 | 修法 |
|---|---|---|
| 图里根本不是那段文字（含状态栏 / 位置偏了） | **选区/坐标**问题 | 调选区或位置微调 |
| 图里确实是对话框正文，但译文对不上 | **模型读错 / 编造** | 改提示词（本次就是这类） |

所以现在每次请求都会把**实际送出去的那块图**落盘，控制页新增一个回看窗口：

> **最近一次送出的图（模型真正看到的）**

看到它就能立刻判定归属，不用再靠猜。这张图同时也让"选区收窄到只框文字行"这类调整有了依据。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `service/LiveTranslateService.kt` | `GAME_HINT` 重写（禁止推测编造、排除状态条）；`translate()` 里把实际送出的 JPEG 落盘到 `cacheDir/live-last-crop.jpg`；新增常量 `LAST_CROP_NAME` |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | 新增"最近一次送出的图"回看窗口（`ivLastCrop`），`onResume` 时刷新 |
| `app/build.gradle.kts` | `versionCode 31→32`、`versionName 1.15.5→1.15.6` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=32 versionName=1.15.6`。

### 请验证

1. **回看窗口**：进控制页应能看到"最近一次送出的图"。**先看这张图是什么**，再看译文。
   - 图里是状态栏/不是对话框 → 选区问题，把选区收窄到只框文字行
   - 图里就是对话框那段日文 → 那这次改的提示词就是关键，看译文是否还编
2. **不应再出现**"体力提升了50"这类和画面无关的译文。若某句读不清，**宁可不出译文**，也不要给假的。
3. 漏译变多是**预期内的代价**：点阵字确实看不清时，模型现在会选择不说，而不是猜。
   如果漏译太严重，说明选区里混了干扰（状态条/立绘），收窄选区会明显改善。

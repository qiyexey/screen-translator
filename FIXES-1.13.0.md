# 首次引导 · v1.12.0 → v1.13.0

新增**首次启动引导**：把"开权限"从"自己找设置"变成"一步步被带过去"。

---

## 设计取向：不是介绍页，是权限向导

这个 App **不给无障碍 + 悬浮窗就完全不能用**。所以引导的每一步都带一个能直接跳去
开权限的按钮，并在用户从系统设置返回时**实时回报当前状态**（✅ 已开 / ❌ 未开）——
用户不必自己判断"我到底开好了没"。

| 步骤 | 内容 | 可操作 |
|---|---|---|
| 1 | 欢迎 + 六种用法概览 | — |
| 2 | **开启无障碍服务**（核心权限，附带 Android 13+「受限设置」解锁指引） | 去开启 → 实时状态 |
| 3 | **授予悬浮窗权限**（没有它翻译能跑但看不到结果） | 去授权 → 实时状态 |
| 4 | **防止服务被杀**（电池优化白名单） | 一键忽略 → 实时状态 |
| 5 | **配置翻译引擎**（列了国内可直连 + 带免费额度的三家） | 去设置 → 实时状态 |
| 6 | 悬浮球五种手势图解 | 开始使用 |

底部：`跳过` / `‹ 上一步` / `下一步 ›`，最后一步变 `开始使用`。

---

## "只出现一次" 的实现

- 新增偏好 `App.prefs.onboardingDone`（默认 false）
- `MainActivity.onCreate`：`if (!onboardingDone) startActivity(OnboardingActivity)`
- **跳过与走完都置位** —— 用户明确表示不想看之后不再打扰
- 想看第二次：`设置 → 关于与用法 →「重新查看引导」`（手动入口，不影响"只自动出现一次"）

> 注意：这里没有做"权限没开就反复弹"。理由：引导只负责**第一次把人领进门**；
> 之后哪项权限掉了，主页的状态卡片会直接显示 ❌，并有一键直达「权限与保活」的入口 ——
> 那条路径比反复弹引导更不打扰、也更准确。

---

## 文件变更

**新增**
- `ui/OnboardingActivity.kt`（374 行，程序化建 UI，单文件自包含，无新增布局/依赖）

**改动**
| 文件 | 改动 |
|---|---|
| `util/Prefs.kt` | 新增 `onboardingDone` |
| `AndroidManifest.xml` | 注册 `OnboardingActivity` |
| `ui/MainActivity.kt` | 首启自动进引导 |
| `ui/BaseActivity.kt` | `requestIgnoreBatteryOptimizations()` 提到基类共用 |
| `ui/PermissionActivity.kt` | 删除重复实现，改用基类 |
| `res/layout/activity_about.xml` + `ui/AboutActivity.kt` | 新增「重新查看引导」入口 |
| `app/build.gradle.kts` | `versionCode 23→24`、`versionName 1.12.0→1.13.0` |

---

## 验证

**首次安装后**打开 App，应自动进入引导第 1 步。

1. 逐步走到第 2 步 → 点「去开启无障碍」→ 系统设置里开启 → **返回 App 应显示「✅ 无障碍服务已开启」**
2. 第 3 步同理，授权后返回应变 ✅
3. 点「跳过」或走完第 6 步 → **杀掉 App 重开，不应再出现引导**
4. 设置 → 关于与用法 →「重新查看引导」→ 应能再次进入

> 若第 1 步没出现：说明 `onboardingDone` 已被置位。可在应用详情里「清除数据」后重试。

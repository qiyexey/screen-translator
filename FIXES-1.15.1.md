# 实时屏幕翻译 · 实测问题修复 · v1.15.0 → v1.15.1

v1.15.0 第一次上真机，反馈四条问题。逐条说明**成因**与**这次的改法**，
并说明其中一条我判断是**误判**，以及怎么用新加的预览一眼分清。

---

## 反馈与诊断

### ① 「框选非常困难」→ 成立，改法最重

原实现直接复用了 `RegionSelectView`：一层全屏遮罩，**压在正在运行的游戏画面上**拖框。
游戏画面一直在动，手指一抖就对不上要框的位置；而且画完**不能改**，只能重来。

改动：新增 `overlay/RoiPickerView.kt` 取代它。

| 改进 | 说明 |
|---|---|
| **冻结帧当背景** | 进框选前先抓一帧冻住，在**静止图**上画。瞄准难度降一个数量级 |
| **四角把手** | 画完还能拖角微调（判定半径 30dp，比把手本身大，手指好按） |
| **3 倍放大镜** | 手指挡住的地方看不见，所以给放大镜 + 十字线；它固定停在选区外侧，不跟着手指抖 |
| **底部「确定 / 取消」** | 不再有"松手即翻译"这种不可撤销的隐式行为 |

### ② 「框没有覆盖在选定的区域」→ 成立，但**根因待定**，先做成"看得见"

这条有两种完全不同的可能，而 v1.15.0 的问题是**用户和排查者都看不出来是哪一种**：

- **A. 坐标换算错**（旋转、notch 遮挡、全屏悬浮窗被系统撑大）→ 叠层落在别处
- **B. 坐标没错，但框选时就没框准**（因为 ① 太难用）→ 叠层落在你手抖画到的位置

改动：
- **框选器里加了真实裁剪预览**（底部小图 "将送出这一块"）。它调用的是服务取帧时
  **同一个** `Roi.toFrameCoords`，所以：**预览里是什么，送去翻译的就是什么。**
  - 预览 ≠ 你框的 → A（坐标换算问题）
  - 预览 = 你框的 → 位置是对的，问题在别处
- **叠层加了半透明绿细描边**：叠层到底盖在哪，一眼可见。深色游戏画面上
  一块深色叠层原本几乎看不出来。
- 框选器会把 `视图尺寸 vs 真实屏幕尺寸 vs 帧尺寸` 打进日志，A 类问题可以直接读出来。

> 我倾向于是 B + ① 的连锁反应：框选难 → 随手画了个位置 → 叠层当然不在你想的地方。
> 但这次不猜了，预览会把答案直接摆出来。

### ③ 「通知栏上没有框选选项」→ 成立，而且是**入口设计失误**

代码里三个动作（⏸ 暂停 / 🔲 框选区域 / ⏹ 停止）**确实都写了**，问题在于：

1. **Android 会把通知动作折叠起来**，默认只显示标题和正文 —— 用户看不到按钮。
2. 更致命的是：v1.15.0 在"没有选区"时**只在叠层上写一句"点通知里的「框选区域」"**，
   而那个按钮恰恰是被折叠的。等于把唯一入口藏进了需要先知道才会展开的地方。
3. 还有一种可能是：你看到的是**悬浮窗服务**那条通知（"屏幕翻译运行中 · 悬浮窗翻译已开启"，
   `OverlayService`，ID 1001），它本来就没有任何动作。实时翻译的通知 ID 是 1003。

改动：
- **无有效选区时，启动后自动弹出框选器** —— 不再要求用户先去通知栏找按钮。
- 通知正文改成明确提示："下拉通知展开「框选区域」等按钮"。

### ④ 「OCR 基本识别不到文字」→ 这条我判断**是误判**，需要你确认

**实时屏幕翻译这条路上没有 OCR。** 它不经过 ML Kit，而是把**裁下来的那块图直接交给视觉模型**
（`translateImage`）。所以：

- 如果你用的是 **📷 拍照翻译** → 那条路确实是 ML Kit OCR，而且用的是**中文识别器**
  （`ChineseTextRecognizerOptions`，依赖里根本没有日文模型），**认不了日文是必然的**。
  这是 v1.14.0 就存在的事实，本次**没有**改它。
- 如果你用的是 **🖼 实时屏幕翻译** → 模型返回"没有识别到文字"最可能的原因是
  **送进去的那块图本身就是空的或错的**（选区错位），而不是模型读不出点阵字。

**怎么一眼分清**（这次新增的预览就是干这个的）：
看框选器底部的"将送出这一块"小图。
- 小图里没有日文（空白/游戏背景/糊边）→ 是选区或坐标问题，**不是模型问题**
- 小图里清楚有日文，但叠层显示"未识别到文字" → **才是模型读不了这种点阵字**，
  那这条技术路线就要重新评估

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `overlay/RoiPickerView.kt` | **新增**（376 行）：冻结帧背景 + 四角把手 + 3 倍放大镜 + **真实裁剪预览** + 确定/取消 |
| `service/LiveTranslateService.kt` | 框选改用 `RoiPickerView`；抓冻结帧时先藏叠层；无有效选区时**自动进入框选**；通知文案写明"下拉展开"；框选期间主循环让路（`picking` 标志） |
| `overlay/LiveOverlayView.kt` | 加半透明绿细描边（让叠层位置可见） |
| `app/build.gradle.kts` | `versionCode 26→27`、`versionName 1.15.0→1.15.1` |

`RegionSelectView` **保持原样未动** —— 它仍在服务既有的"双击悬浮球框选翻译"里用着，
那条路依赖无障碍查文本，坐标体系不同，不动它比较安全。

---

## 验证

### 已确认（编译/打包）

- `gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=27 versionName=1.15.1`

### ⚠️ 仍未确认（运行时）

**这一版依然没有在真机上跑过**（设备控制面被会话档位挡着）。
但这次的改动方向是**把不可见的东西变成可见的**，所以请按下面顺序走一遍，
每一步的结论都能直接把问题二分：

1. 点「开始实时翻译」→ 授权 → **应当自动弹出框选器**（不用去翻通知栏）
   - 没弹 → 服务没起来，请把通知栏截图发我（尤其确认是不是 ID 1003 那条）
2. 框选器里：**应当看到冻结的游戏画面**（不是黑的、不是活的）
   - 看不到冻结帧 → 取帧失败，是另一个问题
3. 拖一个框 → **看底部"将送出这一块"小图**
   - 小图里是**你在框里看到的日文** → 坐标正确，往下走
   - 小图里是别的内容/空白 → **坐标或换算有问题**，把这一步的截图发我
4. 点「确定」→ 叠层应当**正好**盖住你框的那块（有绿色细描边，能看清边缘）
   - 位置偏了 → 把叠层实际位置 vs 你框的位置描述给我
5. 等游戏出对白 → 叠层出现译文
   - 显示"未识别到文字"**但**第 3 步小图里明明有日文 → **模型读不了点阵字**，
     请告诉我用的是哪个引擎/模型，我们换模型或改路线
6. 让对白停 10 秒 → 通知里「已翻译 N 次」**不应**增长

### 请一并告知

- 用的是哪个**引擎/模型**（v1.15.0 只检查了引擎的 `visionCapable` 标记，
  不校验你的 key/中转是否真的支持图片）
- 模拟器是**横屏还是竖屏**跑（旋转会让绝对坐标失效，框选器现在会自动校验并提示重框）

---

## 一个仍未验证的核心疑问

**视觉模型到底认不认得了 GBA 那种 8×8 点阵日文假名** —— 这才是整条路线成不成立的
关键，而它只能靠第 3 步的预览 + 第 5 步的结果来回答。
如果预览正确、模型依然读不出，那就该考虑换路线（例如给模拟器端做逐帧 OCR 预训练模型，
或回到 ROM 汉化那条路），而不是继续在 UI 上打转。

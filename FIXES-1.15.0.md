# 实时屏幕翻译 · 译文原位叠层 · v1.14.0 → v1.15.0

新增第 7 个功能入口：**🖼 实时屏幕翻译**。
面向**模拟器/游戏**这类"画面一直在变、但文字反复出现"的场景：
授权一次屏幕捕获，之后持续取帧 → 画面变了就翻译 → **译文直接压回原文位置**。

---

## 为什么现有两条路都走不通

这不是"再加一个模式"，而是补一个**结构性的空缺**：

| 既有入口 | 拿文字的方式 | 对游戏画面为什么无效 |
|---|---|---|
| 🌐 全屏自动翻译 | 无障碍 `AccessibilityNodeInfo` 节点文本 | 模拟器把游戏画进 Surface，**根本没有文本节点**，一个游戏字都读不到 |
| 🖼 图片翻译 | 抓**一帧** → 手动框选 → 翻译 | 方向对，但每次都要重新弹授权 + 手拖框，是"查字典"，不是"边玩边看" |

本版把后者变成**常驻闭环**：授权一次，之后自动循环。

---

## 设计要点

### 1. 自捕获回环（这个功能最容易踩的坑）

叠层窗口**也会被 MediaProjection 截进去**。不管的话会变成死循环：

```
截到原文 → 翻译 → 叠层盖上 → 下一帧截到的是自己的译文 → 判定"画面变了"
        → 再翻译译文 → 又变 → 无限自我翻译 + 疯狂烧额度
```

系统没有"本窗口不参与截屏"的开关 —— `FLAG_SECURE` 是**反过来**的：
它会把该区域在截图里涂黑，而那块恰好就是选区，等于自废武功。

解法（`LiveOverlayView.setCaptureHidden`）：取帧前把**窗口 alpha 置 0**，
等 `CAPTURE_HIDE_MS = 50ms`（约 3 帧）让合成器翻过去，抓完立刻恢复。
50ms 低于闪烁融合阈值，用户看不见；而抓到的帧一定是**干净的游戏画面**。
叠层没内容时跳过隐藏，省掉这 50ms。

### 2. 省额度：三道闸

视觉请求**按次计费**，"每秒一帧"字面意义上是每小时 3600 次。所以：

| 闸 | 机制 | 挡掉什么 |
|---|---|---|
| **画面没变就不发** | 16×16 灰度指纹 + 差异阈值（默认 8） | 文本停着不动的绝大多数帧（差异通常 < 2） |
| **抖动稳定才发** | 检测到变化后**再等一轮**，要求两轮之间差异 ≤ 12 | 过场动画、翻页中间态那种糊帧 |
| **指纹记忆 LRU(40)** | 指纹命中直接复用上次译文 | 菜单来回切、反复出现的同一句提示 |

另有**单请求在途**约束：上一发没回来就不再发 —— 网络慢时自动退化成低频，
不会把请求堆在队列里越积越多。通知栏常显「已翻译 N 次」，消耗是可见的。

为什么指纹用"降采样成 16×16 再比灰度"而不是逐像素：逐像素对**抖动和抗锯齿**
极度敏感，模拟器开个扫描线滤镜就会让同一段文字每帧都被判成"变了"，
闸门等于失效。16×16 降采样天然吃掉这类噪声，只保留"画面结构变没变"。

### 3. 取帧：降采样 + 复用位图

- 整屏 ARGB 每帧是 10MB 级拷贝（1440×3200 约 18MB），而只用得上文字框那一小块。
  VirtualDisplay 建成长边 ≤ 1600（与上传压缩上限一致），帧小到 1/3 左右。
- 缩放比例**不写死**：`Roi.toFrameCoords` 用的是位图实际尺寸 ÷ 屏幕尺寸，
  所以即便某些 ROM 没按请求尺寸缩放镜像内容，坐标也不会错位。
- 全屏 Bitmap 只分配一次、复用；每帧 new 一个会让 GC 在 1 秒一轮的循环里
  持续抖动（一局游戏上千次）。
- `Image` 必须 `close()`，否则 ImageReader 缓冲（maxImages=2）耗尽，
  表现为"图片突然永远取不到"且不报错。

### 4. 叠层默认**不吃触摸**

叠层精确盖住选区，如果可触摸就会**把游戏那一块操作区变哑**。
默认 `FLAG_NOT_TOUCHABLE`，游戏操作完全不受影响。
需要"按住叠层看原文"的用户可在控制页打开（设置里写明了代价）。

### 5. 选区失效要**如实说**，不能硬截

框选存的是屏幕绝对坐标。旋转屏幕 / 换分辨率（折叠屏展开）后，同一个矩形可能
变成"贴在角落的一条缝"。这种矩形拿去取帧只会得到糊边然后白烧请求，
所以 `Roi.isValidOnScreen` 要求至少 90% 面积在屏内，否则判定失效并提示重框。

### 6. 游戏画面专用提示词

`Translator.translateImage()` 新增可选参数 `hint`（v1.15.0）：
同一个视觉通道要服务两种差别很大的输入 —— 图片翻译是"手机截图、印刷体"，
实时翻译是"模拟器复古游戏、低分辨率点阵字、有扫描线"。对后者，
提示词里说清"这是游戏画面、按短译文处理、糊字要结合上下文推断"，
能明显压低识别失败率和译文啰嗦度。不传 `hint` 就完全沿用原提示词，
所以既有调用点（图片翻译 / 拍照翻译）零改动。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `util/FrameGrabber.kt` | **新增**：常驻 VirtualDisplay + ImageReader 取帧（对比 `ScreenCapture` 的一次性抓帧） |
| `util/FrameSignature.kt` | **新增**：16×16 灰度指纹 + 差异度（0~100）+ 指纹哈希 |
| `util/Roi.kt` | **新增**：选区序列化、屏幕→取帧坐标换算、有效性与旋转失效判定 |
| `util/ImageCompress.kt` | **新增**：上传前压缩（从 `ImageTranslateActivity` 抽出，两处共用） |
| `overlay/LiveOverlayView.kt` | **新增**：原位译文叠层 + 取帧隐藏（自捕获回环） |
| `service/LiveTranslateService.kt` | **新增**：主循环、三道额度闸、框选、前台服务与通知动作 |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | **新增**：控制页（选区 / 节奏 / 开关 / 限制说明） |
| `api/Translator.kt` | `translateImage()` 增加可选 `hint` |
| `api/CachingTranslator.kt` / `OpenAICompatibleTranslator.kt` / `ClaudeTranslator.kt` | 转发并使用 `hint`；系统提示词支持场景补充 |
| `util/Prefs.kt` | 4 个新偏好：`liveRoi` / `liveIntervalMs` / `liveDiffThreshold` / `liveOverlayTouchable` |
| `AndroidManifest.xml` | 注册 `LiveTranslateService`（`foregroundServiceType="mediaProjection"`）与 `LiveTranslateActivity` |
| `ui/MainActivity.kt` + `layout/activity_main.xml` | 第 7 个功能入口 |
| `app/build.gradle.kts` | `versionCode 25→26`、`versionName 1.14.0→1.15.0` |

---

## 验证

### 已确认（编译/打包层面）

- `gradle assembleDebug` **BUILD SUCCESSFUL**（AGP 8.5.2 / Gradle 8.7 / JDK 17）
- APK 内已确认：`versionCode=26 versionName=1.15.0`
- manifest 中 `LiveTranslateService` 的 `foregroundServiceType=0x20`
  （= `FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION`）
- dex 中已确认 `LiveTranslateService` 类存在
- 已用 **v2 方案签名**（`APK Sig Block 42` 在文件尾）

### ⚠️ 未确认（运行时行为）

**我没有在真机上跑过它** —— 当时会话档位是 `workspace-write`，设备控制面被挡，
无法安装/启动/观察。所以下面这些**必须你实测**：

1. 选一个**空文本框区域**（游戏没出文字时）框选 → 应显示「未识别到文字」，不崩
2. 让游戏出一段对白 → 应在 1~2 个间隔内出现译文，**压在原文位置上**
3. **喂一眼原文**：确认叠层确实盖住了原位置；再确认**没有**发生"译文被自己
   反复重译"（如果发生，说明取帧隐藏没生效，看日志里 `[实时] 第 N 次` 是否疯涨）
4. 让对白停着不动 10 秒 → `已翻译 N 次` **不应**增长（闸门 1 生效）
5. 关掉视觉引擎（切到 Google/DeepL 等）→ 点开始应弹「当前引擎不支持图片」
6. 旋转屏幕 → 应提示"选区已失效，请重新框选"，而不是白烧请求
7. 通知栏的 ⏸/🔲/⏹ 三个动作都要生效；框选时不用切出游戏

### 已知限制（已写进控制页文案）

- 视觉模型每轮 1~3 秒延迟 → **剧情/菜单够用，实时比赛一闪而过的提示跟不上**
- 每次请求都计费；屏幕旋转后需重框
- `FLAG_SECURE` 的应用（部分银行/流媒体）无法捕获，画面会是黑的
- Android 14+ MediaProjection 授权**每次会话都要重新弹窗**（平台规定，绕不过）

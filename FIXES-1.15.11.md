# 实时屏幕翻译 · 阈值重新定标（真正修掉"停在上一段"） · v1.15.10 → v1.15.11

v1.15.10 把 Δ（画面差异值）显示到叠层上之后，真机数据第一次摆到了明面上：

| 时刻 | Δ | 用户设的阈值 | 结果 |
|---|---|---|---|
| 15:13:14（整句日文换掉） | **Δ4** | 阈5 | 4 < 5 → **判"没变"，不翻译** |
| 15:13:48 | **Δ2** | 阈5 | 这一句译了（译18→19） |

两个结论，第二个是病根：

1. **指纹修好了** —— Δ 不再是恒 0，说明 v1.15.10 把 Canvas 缩图换成直接采样是对的。
2. **Δ 的量级是我当初估错了 5 倍。** 我在注释里写"换成新的一段对白 → Δ 10~40"，
   而实测整句对白换掉只有 **2~5**，噪声（抖动 / 闪烁光标）是 **0**。
   于是默认阈值 8 **让每一次真实变化都够不到** —— 表现就是"只翻译第一段，之后停在上一段"。

**这才是"停在上一段"的真正原因**（前面几版改的是度量与闸门，方向对但阈值定标错了）。

---

## 改动

### 1. 默认阈值 8 → **1**

真机数据给出的分界线就在 1 附近：噪声 0，真实变化 2~5。
（我原来按"10~40"估的，差了一个数量级。）

### 2. 阈值可调范围 2~50 → **1~20**

原范围的下限 2 就已经高于噪声了，上限 50 毫无意义。

### 3. 格子判定阈值 `CELL_DELTA` 24 → **12**

让真实变化的 Δ 量级大致翻倍（2~5 → 5~10 量级），与噪声（0）之间的间距更宽裕，
不贴着分界线走。

### 4. **一次性迁移**：老用户存下的旧阈值统一重置

用户已经存下了 5（或 8），不重置的话升级后**照样卡住**，而且从界面上完全看不出
原因（滑杆显示 5，看着很合理）。所以升级时统一重置成新默认，沿用本项目既有的
`migratedVxxxx` 迁移写法。

### 5. 修掉译文中途被截断

截图里 `各モニタリング良好。` 只译出「各显示器」—— 因为贴边模式下我把叠层高度
设成了**和选区一样高**，而日文一句译成中文往往要两三行，装不下就被裁掉。
现在贴边模式给足高度（选区高度 ~ 屏高 1/3 之间）。贴边模式不参与取帧，给高一点无副作用。

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `util/FrameSignature.kt` | `CELL_DELTA 24→12`；用真机数据重写典型取值注释 |
| `util/Prefs.kt` | `liveDiffThreshold` 默认 8→1、范围 2~50→1~20；新增 `DEFAULT_LIVE_DIFF`、`migratedV11510` |
| `App.kt` | 执行 v1.15.10 迁移：重置旧阈值 |
| `service/LiveTranslateService.kt` | 贴边模式叠层高度改为"选区高度 ~ 屏高 1/3"，避免译文截断 |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | 滑杆范围同步为 1~20；标签写明实测 Δ 量级 |
| `app/build.gradle.kts` | `versionCode 36→37`、`versionName 1.15.10→1.15.11` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=37 versionName=1.15.11`。

### 请验证

1. **升级后阈值滑杆应在 1 附近**（迁移生效）。
2. 让游戏连续换 4~5 段对白 → **每段都应出译文**，不再停在上一段。
3. **叠层左上角那行 Δ 读数要继续看**：
   - 换对白时 Δ 应变成 **5~10 量级**（CELL_DELTA 调小后）
   - 文本停着不动时 Δ 应为 **0**
4. **译文不应再被截断**（叠层变高了）。

> 如果换对白时 Δ 仍然只有 2~3 且偶发不触发，把阈值再往下压到 1 之前，
> 请先把 Δ 读数发我 —— 那说明还有别的因素在稀释差异（例如选区里混进了大片
> 静态背景），正确的解法是**把选区收窄到只框文字行**，而不是继续压阈值。

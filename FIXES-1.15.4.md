# 实时屏幕翻译 · 循环保活与位置校正 · v1.15.3 → v1.15.4

两条反馈：**①「只识别到第一句，后续都没反应」**、**②「叠层位置和框选位置有偏差」**。

① 查到两个**会让流程永久停摆**的缺陷（主因在第一条）；② 查到窗口 flags 的经典成因，
并补了一个用户可直接用的校正口。

---

## ① 「只识别第一句，后续没反应」

### 主因：主循环是"光着调"的，抛一次异常就整个死掉

```kotlin
// 旧
loopJob = scope.launch {
    while (isActive && running) {
        delay(...)
        tick()          // ← 没有任何保护
    }
}
```

`tick()` 内部任何一次异常（取帧、指纹计算、窗口更新、翻译器构造……）都会让这个协程
**直接结束**。而它承担的是整个功能的心跳，所以表现为：

> **第一句翻译成功之后，循环已经悄悄死了，界面上再也什么都没有。**

更糟的是**没有任何痕迹**：叠层还停在上一段译文，通知里计数也不动，用户完全无法判断
是"卡住"还是"没识别到"。

**改法**：单轮异常一律捕获并继续，同时如实告知（前 3 次会在叠层上写"已自动恢复（第 N 次）"）。
`CancellationException` 单独放行 —— 它是正常取消信号，不该被当成故障。

### 次因：自捕获校验可能永久停摆

v1.15.2 引入的"抓到自己就不认账"在**藏叠层始终失效**的 ROM 上会变成死循环：
每一轮都判定"抓到自己"→ 每一轮都 `return` → 同样表现为"只翻译了第一句"。

**改法**：连续 `POLLUTED_MAX_HITS = 8` 轮都判定抓到自己，就**放弃该判据**并明确提示。
取舍理由：宁可偶尔把自己的译文翻进去（**可见、可调**：把翻译区域挪开叠层即可），
也不要一动不动（**不可见、无从下手**）。停摆比误判更糟。

---

## ② 「叠层位置和框选位置有偏差」

### 成因一：`FLAG_LAYOUT_NO_LIMITS` 让窗口原点跑到屏幕外

框选遮罩和译文叠层都带着这个 flag。它的语义是"允许窗口超出屏幕范围"，
于是**窗口原点不一定是屏幕的 (0,0)**。而整个坐标体系建立在
"视图内坐标 == 屏幕绝对坐标"这个前提上（`RegionSelectView` 的注释里也这么写着）——
前提不成立，就必然出现一个**常量偏移**。

**改法**：两处都去掉 `FLAG_LAYOUT_NO_LIMITS`，只保留 `FLAG_LAYOUT_IN_SCREEN`。

### 成因二：窗口更新在错误的线程上执行，而且失败了没人知道

`wm.updateViewLayout()` 会触碰视图层级，**必须在 UI 线程调用**。而它原先是在主循环的
工作线程上调用的，还裹着 `runCatching { }` **把异常吞了** —— 一旦抛
`CalledFromWrongThreadException`，"位置没更新""叠层没隐藏"都会**静默失效**，
日志里连一行都不会有（这也是前几轮一直查不出问题的原因之一）。

**改法**：统一 post 到 UI 线程执行，失败写日志：
```kotlin
private fun updateWindow(wm: WindowManager, what: String) { ... }
```

### 兜底：位置微调 + 自带几何读数

不同 ROM 对全屏悬浮窗的原点处理确实不一致（状态栏、挖孔、导航栏）。
与其赌某个 ROM 的规则，不如给一个用户**直接能用**的校正口：

- **控制页新增「译文位置微调 X / Y」**（±80dp）。调到 0 位是默认；拖动即时生效
  （新增 `ACTION_REPOSITION`，不用等下一轮 tick）。
- **选完区域后，叠层会打印几何读数**：
  `区域已保存 1000×280` / `视口 1256×2760｜屏 1256×2760｜微调 0,0`
  —— **视口 ≠ 屏** 就说明坐标基准不同源，这是位置偏移的根因判定依据。
  （之所以写进叠层而不是日志：App 的日志没有 `READ_LOGS` 权限读不到，写进叠层就能截图看。）

---

## 文件变更

| 文件 | 改动 |
|---|---|
| `service/LiveTranslateService.kt` | 主循环兜异常（`loopFailures`）；自捕获判据连续 8 次即放宽（`pollutedHits`）；新增 `overlayRect()` 应用微调；新增 `screenMetrics()`；新增 `ACTION_REPOSITION`；选完区域后输出几何读数 |
| `overlay/LiveOverlayView.kt` | 去掉 `FLAG_LAYOUT_NO_LIMITS`；新增 `updateWindow()`（UI 线程 + 失败写日志），替换两处静默 `runCatching` |
| `overlay/RoiPickerView.kt` | 去掉 `FLAG_LAYOUT_NO_LIMITS`，并写明为什么刻意不加 |
| `util/Prefs.kt` | 新增 `liveNudgeX` / `liveNudgeY` |
| `ui/LiveTranslateActivity.kt` + `layout/activity_live_translate.xml` | 新增两个位置微调滑杆（±80dp，拖动即时生效） |
| `app/build.gradle.kts` | `versionCode 29→30`、`versionName 1.15.3→1.15.4` |

---

## 验证

### 已确认（编译/打包）

`gradle assembleDebug` **BUILD SUCCESSFUL**，`versionCode=30 versionName=1.15.4`。

### 请这样验

**① 循环是否还活着**
让游戏连续出 **4~5 段**对白。每段都应出译文。
若中途又停了，叠层这次会写明原因（"⚠️ 已自动恢复（第 N 次）+ 异常名"），把那句话告诉我。

**② 位置偏差**
1. 重新框选一次区域。选完看叠层正文那行几何读数，把 `视口` / `屏` 两个数报给我
   —— **如果两者不等，我就知道该怎么在代码里换算，而不是继续猜**。
2. 然后看叠层是否正好盖住选区。
3. 若仍偏，用控制页的 **X / Y 微调**拖到重合，把最终数值告诉我
   —— 我可以把它固化成一个按"视口−屏"差值自动计算的偏移，省掉手动调。

> 说明：这一版**去掉了 `FLAG_LAYOUT_NO_LIMITS`**，本身就很可能让偏差直接消失；
> 微调是兜底，读数是为了把根因钉死。三个一起上，是为了不再多耗你一轮。
